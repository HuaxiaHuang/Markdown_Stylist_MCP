mcp>=1.0.0
markdown>=3.7
Jinja2>=3.1
beautifulsoup4>=4.12
lxml>=5.0
latex2mathml>=3.77

# PDF engine. Playwright is preferred because Chromium renders MathML well.
playwright>=1.48

# Optional PDF fallback. On Windows, WeasyPrint may require extra GTK runtime.
WeasyPrint>=62.0
